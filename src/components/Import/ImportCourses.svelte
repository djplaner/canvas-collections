<script>
	/**
	 * @file ImportCourses.svelte
	 * @description Display useful information about the two courses we've been provided
	*/
</script>


<h3>🏗️ ImportCourses.svelte</h3>

<p>
	/**
 * @class CollectionsDetails<br />
 * @description Given a course Id retrieve and possibly update the content of<br />
 * the Canvas Collections Configuration page<br />
 *<br />
 * Process here is<br />
 * - requestConfigPageContents<br />
 *   Ask to get the contents of the page<br />
 * - if successful<br />
 *   - TODO check to see if it's moved from other course (and other checks)<br />
 *      - only if in edit mode<br />
 *   - parse the JSON into a data structure<br />
 *   - TODO retrieve last collection viewed<br />
 * - if not successful (i.e. page doesn't exist)<br />
 *   (only if edit mode)<br />
 *   - initialise config page<br />
 *   - save the config page<br />
 */
</p>